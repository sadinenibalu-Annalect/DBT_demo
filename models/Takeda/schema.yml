
version: 2

models:
    - name: takeda_test
      description: "A starter dbt model"
      columns:
          - name: seq
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
          - name: name
            description: "The name of seq"